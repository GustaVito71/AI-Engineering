# SDKs de cada proveedor
openai>=1.40
anthropic>=0.40
google-genai>=1.0
deepseek>=1.0
# Carga de variables de entorno desde .env
python-dotenv>=1.0
# Base de datos local de precios de modelos (costo estimado por llamada)
llmprice-kit>=2026.4
# Monitoreo de errores en la nube
sentry-sdk>=2.0
