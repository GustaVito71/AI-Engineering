# Copia este archivo a .env y añade tus keys:  cp .env.example .env
# NUNCA subas tu .env real a git: sus valores se consideran secretos.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
DEEPSEEK_API_KEY=

# Proveedor por defecto al ejecutar switcherllm.py
LLM_DEFAULT=anthropic

# Rol (system prompt) por defecto del asistente
LLM_ROLE=Eres un asistente breve que responde en español.